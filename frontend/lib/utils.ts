import { clsx, type ClassValue } from "clsx"; import { twMerge } from "tailwind-merge"; const mergeClasses = (...inputs: ClassValue[]) => clsx(inputs); export const cn = (...inputs: ClassValue[]) => twMerge(mergeClasses(...inputs));