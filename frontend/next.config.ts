import type { NextConfig } from "next"; function getImagesConfig(): NextConfig['images'] { return { domains: ['example.com'], formats: ['image/webp'],};} function getI18nConfig(): NextConfig['i18n'] { return { locales: ['en', 'fr'], defaultLocale: 'en',};} const nextConfig: NextConfig = { images: getImagesConfig(), i18n: getI18nConfig(), /* other config options here */}; export default nextConfig;