import { clsx, type ClassValue } from "clsx"; import { twMerge } from "tailwind-merge"; const mergeClasses = (inputs: ClassValue[]) => twMerge(clsx(inputs)); export function cn(...inputs: ClassValue[]) { return mergeClasses(inputs); }